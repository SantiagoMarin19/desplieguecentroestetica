export const Input = ({type}) => {
    return(
        <div className='contenedorCorreo'>
                    <input className="inputcontenedores" type={""} name='correo'  placeholder="Ingrese su correo"  onChange={handleChange}/>
                    </div>
    )
}